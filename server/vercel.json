{
  "version": 2,
  "builds": [
    {
      "src": "server/cmd/api/main.go",
      "use": "@vercel/go"
    }
  ],
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/server/cmd/api/main.go"
    }
  ]
}
